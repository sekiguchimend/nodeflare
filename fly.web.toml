# Fly.io configuration for Web Frontend
app = "mcp-cloud-web"
primary_region = "nrt"

[build]
  dockerfile = "apps/web/Dockerfile"
  [build.args]
    NEXT_PUBLIC_API_URL = "https://mcp-cloud-api.fly.dev"

[env]
  NODE_ENV = "production"
  API_URL = "http://mcp-cloud-api.internal:8080"

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = false
  auto_start_machines = true
  min_machines_running = 1

  [http_service.concurrency]
    type = "connections"
    hard_limit = 250
    soft_limit = 200

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512
